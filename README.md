# whenever-design

Next.js site lives in **`whenevr/`**.

- **Local:** from this folder run `npm install`, then `npm run dev` or `npm run build`.
- **Vercel:** set **Root Directory** to **`whenevr`** (Project → Settings → General).

See `whenevr/README.md` for details.
